#include "Save.hpp"

void save(const std::string lvl_path) {
    (void)lvl_path;
    // Add player config also as a input param
}

void load(const std::string lvl_path) {
    (void)lvl_path;
    // Add player config also as a input param
}
